package com.dartslive.stamp;

public class AppConst {	
	public static final String SVC_CD = "S0006";
	public static final boolean isTestServer = false; // true : Test Server, false : Live Server 
	public static final boolean isDev = false; 		 // true : Dev Version(use debug keystore), false : Release Version(use distribution keystore) 
}
